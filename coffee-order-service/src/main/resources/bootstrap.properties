# Define the name of your Spring application
spring.application.name=coffee-order-service

# Specify the URI of your Spring Cloud Config server
spring.cloud.config.uri=http://localhost:8888

# Specify the active profile for your application
spring.profiles.active=default

# If you're not using Eureka for service discovery, comment out or remove this line
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka

